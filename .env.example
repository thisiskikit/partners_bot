# Backend
PORT=3000
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-2.0-flash
DB_PATH=./server/app.db

# Optional compatibility alias if your environment expects DATABASE_URL
# sqlite file paths can be mapped externally if needed.
DATABASE_URL=

# Frontend (optional)
VITE_API_BASE_URL=
